/**
 * Core event names for cross-plugin communication.
 * Mirrors main/plugin-system/EventBus CoreEvents for renderer use.
 */
export const CoreEvents = {
  SEARCH_OPENED: "ui:search:opened",
} as const;
