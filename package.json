{
  "name": "mindscape-outliner",
  "version": "0.1.5",
  "description": "WorkFlowy-like desktop outliner built with Electrobun and SQLite",
  "scripts": {
    "start": "bun run build:dev && electrobun dev",
    "build:dev": "bun install && electrobun build",
    "build": "electrobun build"
  },
  "dependencies": {
    "electrobun": "latest",
    "preact": "^10.19.0",
    "htm": "^3.1.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
